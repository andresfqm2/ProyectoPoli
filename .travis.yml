language: java
sudo: false
script: 
- mvn clean verify
- copy target\RestauranteAPPV2.war C:\home\wildfly-8.2.0.Final\standalone\deployments\RestauranteAPPV2.war
deploy:
provider: pages
skip-cleanup: true
github-token: $GITHUB_TOKEN
local_dir: build
on:
branch: master